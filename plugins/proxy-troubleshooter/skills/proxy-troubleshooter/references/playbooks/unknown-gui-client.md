# Playbook: Unknown GUI Client

Use when a proxy core appears to be running but the GUI wrapper family is unknown.

Check:

- Process names and executable paths.
- Listening ports.
- Safe app-data directory names.
- Non-sensitive config fingerprints.

Do not read broad config directories or controller APIs without explaining why and asking for approval.
